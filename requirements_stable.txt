# STABLE REQUIREMENTS - Project Alpha 1 / HandSignify
# Use with Python 3.11 for best compatibility (Python 3.13 has known issues)
#
# Root cause fixes:
# - Flask 2.3.3: Avoids "RequestContext session has no setter" (Flask 3.x incompatibility)
# - eventlet 0.33.3: Avoids deprecation warnings and Windows ConnectionAbortedError
# - scikit-learn 1.3.0: Matches trained model (avoids InconsistentVersionWarning)

# Core ML/CV Stack
opencv-python==4.8.0.74
mediapipe==0.10.32
numpy>=1.24.3,<2

# Flask Stack (pinned for Flask-SocketIO compatibility)
Flask==2.3.3
Werkzeug>=2.3.0,<3
Flask-Cors==4.0.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Bcrypt==1.0.1
Flask-Mail==0.9.1

# WebSocket Stack (pinned - newer versions have Flask 3 session issues)
flask-socketio==5.3.6
python-socketio==5.8.0
python-engineio==4.7.1
eventlet==0.33.3

# Forms and Validation
WTForms==3.0.1
email-validator>=2.1.0

# Security
itsdangerous>=2.1.2

# ML Libraries (1.3.0 matches trained model.p)
scikit-learn==1.3.0
matplotlib>=3.7.2

# HTTP client
requests>=2.28.0
